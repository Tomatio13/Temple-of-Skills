# Offline Fonts

## 目的

オフライン再生時のフォント差分を減らすため、Webフォント参照をローカルフォント参照に置き換える。

## 必要ファイル

- `*.woff2` フォントファイル
- ライセンス文書（例: `OFL.txt`）

## 運用

- `--font-dir` で `.woff2` を指定する。
- `--font-license` でライセンス文書を指定する。
- 未指定の場合はフォント同梱なしで処理される。
