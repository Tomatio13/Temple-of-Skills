# Video Mapping Input Format

ページごとの動画指定は次のいずれかで列挙する。

- `1: videos/intro.mp4`
- `2ページ目は videos/problem.mp4`
- `3 videos/solution.mp4`

注意:

- ページ番号は 1 始まり。
- 全ページ分を必ず指定する。
- 出力Marp内の `src` は指定文字列をそのまま使う。
